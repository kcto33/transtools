<Window x:Class="ScreenTranslator.Windows.PinWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        UseLayoutRounding="True"
        SnapsToDevicePixels="True"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        Topmost="True"
        ShowInTaskbar="False"
        ResizeMode="NoResize"
        ShowActivated="False">
  <Border x:Name="Chrome"
          Background="Transparent"
          BorderBrush="{StaticResource AccentBrush}"
          BorderThickness="1"
          CornerRadius="0">
    <Border.ContextMenu>
      <ContextMenu>
        <MenuItem Header="{DynamicResource PinMenu_Copy}" Click="CopyMenuItem_Click" />
        <MenuItem Header="{DynamicResource PinMenu_Save}" Click="SaveMenuItem_Click" />
        <Separator />
        <MenuItem Header="{DynamicResource PinMenu_Close}" Click="CloseMenuItem_Click" />
      </ContextMenu>
    </Border.ContextMenu>

    <Grid>
      <ScrollViewer x:Name="ImageScrollViewer"
                    Background="Transparent"
                    HorizontalScrollBarVisibility="Hidden"
                    VerticalScrollBarVisibility="Hidden"
                    CanContentScroll="False"
                    PanningMode="None">
        <Image x:Name="PinnedImage"
               Stretch="Fill"
               SnapsToDevicePixels="True"
               RenderOptions.BitmapScalingMode="HighQuality">
          <Image.LayoutTransform>
            <ScaleTransform x:Name="ImageScaleTransform" ScaleX="1" ScaleY="1" />
          </Image.LayoutTransform>
        </Image>
      </ScrollViewer>

      <Button x:Name="CloseButton"
              Content="x"
              HorizontalAlignment="Right"
              VerticalAlignment="Top"
              Width="24"
              Height="24"
              Margin="2"
              FontSize="12"
              Foreground="White"
              Background="#CC000000"
              BorderThickness="0"
              Cursor="Hand"
              Visibility="Collapsed"
              Click="CloseButton_Click">
        <Button.Template>
          <ControlTemplate TargetType="Button">
            <Border Background="{TemplateBinding Background}" CornerRadius="12">
              <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
            </Border>
          </ControlTemplate>
        </Button.Template>
      </Button>
    </Grid>
  </Border>
</Window>
