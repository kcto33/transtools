<Window x:Class="ScreenTranslator.Windows.ScreenshotOverlayWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        Topmost="True"
        ShowInTaskbar="False"
        ResizeMode="NoResize"
        Focusable="True">
  <Window.Resources>
    <Style x:Key="ToolbarButton" TargetType="Button">
      <Setter Property="Background" Value="Transparent" />
      <Setter Property="Foreground" Value="White" />
      <Setter Property="BorderThickness" Value="0" />
      <Setter Property="Padding" Value="8,4" />
      <Setter Property="Margin" Value="2,0" />
      <Setter Property="Cursor" Value="Hand" />
      <Setter Property="FontSize" Value="12" />
      <Setter Property="Template">
        <Setter.Value>
          <ControlTemplate TargetType="Button">
            <Border x:Name="border" Background="{TemplateBinding Background}" CornerRadius="3" Padding="{TemplateBinding Padding}">
              <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
            </Border>
            <ControlTemplate.Triggers>
              <Trigger Property="IsMouseOver" Value="True">
                <Setter TargetName="border" Property="Background" Value="#55FFFFFF" />
              </Trigger>
              <Trigger Property="IsPressed" Value="True">
                <Setter TargetName="border" Property="Background" Value="#33FFFFFF" />
              </Trigger>
            </ControlTemplate.Triggers>
          </ControlTemplate>
        </Setter.Value>
      </Setter>
    </Style>

    <Style x:Key="ColorPaletteButton" TargetType="Button">
      <Setter Property="Width" Value="14" />
      <Setter Property="Height" Value="14" />
      <Setter Property="Margin" Value="2" />
      <Setter Property="Padding" Value="0" />
      <Setter Property="BorderBrush" Value="#66FFFFFF" />
      <Setter Property="BorderThickness" Value="1" />
      <Setter Property="Cursor" Value="Hand" />
      <Setter Property="Template">
        <Setter.Value>
          <ControlTemplate TargetType="Button">
            <Border x:Name="border"
                    Background="{TemplateBinding Background}"
                    BorderBrush="{TemplateBinding BorderBrush}"
                    BorderThickness="{TemplateBinding BorderThickness}"
                    CornerRadius="1" />
            <ControlTemplate.Triggers>
              <Trigger Property="IsMouseOver" Value="True">
                <Setter TargetName="border" Property="BorderBrush" Value="White" />
              </Trigger>
            </ControlTemplate.Triggers>
          </ControlTemplate>
        </Setter.Value>
      </Setter>
    </Style>

    <Style x:Key="CompactAnnotationSlider" TargetType="Slider">
      <Setter Property="Height" Value="14" />
      <Setter Property="Template">
        <Setter.Value>
          <ControlTemplate TargetType="Slider">
            <Grid Height="14">
              <Track x:Name="PART_Track" VerticalAlignment="Center">
                <Track.DecreaseRepeatButton>
                  <RepeatButton Height="4" Background="#FF7DD2FF" BorderThickness="0" IsTabStop="False" />
                </Track.DecreaseRepeatButton>
                <Track.IncreaseRepeatButton>
                  <RepeatButton Height="4" Background="#33FFFFFF" BorderThickness="0" IsTabStop="False" />
                </Track.IncreaseRepeatButton>
                <Track.Thumb>
                  <Thumb Width="12" Height="12">
                    <Thumb.Template>
                      <ControlTemplate TargetType="Thumb">
                        <Ellipse Fill="#FFEAF8FF" Stroke="#FF7DD2FF" StrokeThickness="2" />
                      </ControlTemplate>
                    </Thumb.Template>
                  </Thumb>
                </Track.Thumb>
              </Track>
            </Grid>
          </ControlTemplate>
        </Setter.Value>
      </Setter>
    </Style>
  </Window.Resources>

  <Grid>
    <Image x:Name="BackgroundImage"
           HorizontalAlignment="Left"
           VerticalAlignment="Top"
           Stretch="Fill" />

    <Canvas x:Name="OverlayCanvas">
      <Path x:Name="DarkOverlay" Fill="#66000000" />
    </Canvas>

    <Canvas x:Name="SelectionCanvas">
      <Canvas x:Name="EditSurface"
              Background="Transparent"
              ClipToBounds="True"
              Visibility="Collapsed">
        <Image x:Name="SelectedImagePreview" Stretch="Fill" />
        <Path x:Name="AnnotationStrokePreview"
              Stroke="#FF00B7FF"
              StrokeThickness="3"
              StrokeStartLineCap="Round"
              StrokeEndLineCap="Round"
              StrokeLineJoin="Round"
              Fill="Transparent"
              Visibility="Collapsed" />
        <Rectangle x:Name="AnnotationRectanglePreview"
                   Stroke="#FF00B7FF"
                   StrokeThickness="3"
                   Fill="Transparent"
                   Visibility="Collapsed" />
        <TextBox x:Name="AnnotationTextBox"
                 MinWidth="80"
                 Background="#CC000000"
                 BorderBrush="#AAFFFFFF"
                 BorderThickness="1"
                 Foreground="White"
                 Padding="4,2"
                 Visibility="Collapsed"
                 AcceptsReturn="False"
                 KeyDown="AnnotationTextBox_KeyDown"
                 LostFocus="AnnotationTextBox_LostFocus" />
      </Canvas>

      <Rectangle x:Name="SelectionRect"
                 Stroke="#FF00B7FF"
                 StrokeThickness="2"
                 Fill="Transparent"
                 IsHitTestVisible="False"
                 Visibility="Collapsed" />

      <Border x:Name="SizeIndicator"
              Background="#CC000000"
              CornerRadius="3"
              Padding="6,3"
              Visibility="Collapsed">
        <TextBlock x:Name="SizeText"
                   Foreground="White"
                   FontSize="12"
                   FontFamily="Consolas" />
      </Border>

      <Border x:Name="Toolbar"
              Background="#EE333333"
              CornerRadius="4"
              Padding="4"
              Visibility="Collapsed">
        <StackPanel Orientation="Horizontal">
          <Button x:Name="BtnSave" Content="{DynamicResource Screenshot_Save}" Style="{StaticResource ToolbarButton}" Click="BtnSave_Click" ToolTip="{DynamicResource Screenshot_Tooltip_Save}" />
          <Button x:Name="BtnCopy" Content="{DynamicResource Screenshot_Copy}" Style="{StaticResource ToolbarButton}" Click="BtnCopy_Click" ToolTip="{DynamicResource Screenshot_Tooltip_Copy}" />
          <Button x:Name="BtnLongScreenshot" Content="{DynamicResource Screenshot_Long}" Style="{StaticResource ToolbarButton}" Click="BtnLongScreenshot_Click" ToolTip="{DynamicResource Screenshot_Tooltip_Long}" />
          <Button x:Name="BtnGif" Content="{DynamicResource Screenshot_Gif}" Style="{StaticResource ToolbarButton}" Click="BtnGif_Click" ToolTip="{DynamicResource Screenshot_Tooltip_Gif}" />
          <Button x:Name="BtnRedraw" Content="{DynamicResource Screenshot_Redraw}" Style="{StaticResource ToolbarButton}" Click="BtnRedraw_Click" ToolTip="{DynamicResource Screenshot_Tooltip_Redraw}" />
          <Button x:Name="BtnPin" Content="{DynamicResource Screenshot_Pin}" Style="{StaticResource ToolbarButton}" Click="BtnPin_Click" ToolTip="{DynamicResource Screenshot_Tooltip_Pin}" />
          <Button x:Name="BtnBrush" Content="{DynamicResource Screenshot_Annotate_Brush}" Style="{StaticResource ToolbarButton}" Click="BtnBrush_Click" ToolTip="{DynamicResource Screenshot_Tooltip_Annotate_Brush}" />
          <Button x:Name="BtnText" Content="{DynamicResource Screenshot_Annotate_Text}" Style="{StaticResource ToolbarButton}" Click="BtnText_Click" ToolTip="{DynamicResource Screenshot_Tooltip_Annotate_Text}" />
          <Button x:Name="BtnRectangle" Content="{DynamicResource Screenshot_Annotate_Rectangle}" Style="{StaticResource ToolbarButton}" Click="BtnRectangle_Click" ToolTip="{DynamicResource Screenshot_Tooltip_Annotate_Rectangle}" />
          <Button x:Name="BtnArrow" Content="{DynamicResource Screenshot_Annotate_Arrow}" Style="{StaticResource ToolbarButton}" Click="BtnArrow_Click" ToolTip="{DynamicResource Screenshot_Tooltip_Annotate_Arrow}" />
          <Border Width="1" Margin="4,2" Background="#66FFFFFF" />
          <StackPanel Width="152" Margin="2,0" VerticalAlignment="Center">
            <Slider x:Name="AnnotationSizeSlider"
                    Style="{StaticResource CompactAnnotationSlider}"
                    Minimum="1"
                    Maximum="10"
                    Value="3"
                    TickFrequency="1"
                    IsSnapToTickEnabled="True"
                    Margin="2,0,2,1"
                    ValueChanged="AnnotationSizeSlider_ValueChanged"
                    PreviewMouseWheel="AnnotationSizeSlider_PreviewMouseWheel"
                    ToolTip="Size" />
            <WrapPanel HorizontalAlignment="Center">
              <Button x:Name="BtnColorBlue" Style="{StaticResource ColorPaletteButton}" Background="#FF00B7FF" Tag="#FF00B7FF" Click="BtnAnnotationColor_Click" ToolTip="Blue" />
              <Button x:Name="BtnColorRed" Style="{StaticResource ColorPaletteButton}" Background="#FFFF0000" Tag="#FFFF0000" Click="BtnAnnotationColor_Click" ToolTip="Red" />
              <Button x:Name="BtnColorOrange" Style="{StaticResource ColorPaletteButton}" Background="#FFFFA500" Tag="#FFFFA500" Click="BtnAnnotationColor_Click" ToolTip="Orange" />
              <Button x:Name="BtnColorYellow" Style="{StaticResource ColorPaletteButton}" Background="#FFFFFF00" Tag="#FFFFFF00" Click="BtnAnnotationColor_Click" ToolTip="Yellow" />
              <Button x:Name="BtnColorGreen" Style="{StaticResource ColorPaletteButton}" Background="#FF32CD32" Tag="#FF32CD32" Click="BtnAnnotationColor_Click" ToolTip="Green" />
              <Button x:Name="BtnColorWhite" Style="{StaticResource ColorPaletteButton}" Background="#FFFFFFFF" Tag="#FFFFFFFF" Click="BtnAnnotationColor_Click" ToolTip="White" />
              <Button x:Name="BtnColorBlack" Style="{StaticResource ColorPaletteButton}" Background="#FF000000" Tag="#FF000000" Click="BtnAnnotationColor_Click" ToolTip="Black" />
              <Button x:Name="BtnColorPurple" Style="{StaticResource ColorPaletteButton}" Background="#FF9370DB" Tag="#FF9370DB" Click="BtnAnnotationColor_Click" ToolTip="Purple" />
            </WrapPanel>
          </StackPanel>
          <Border Width="1" Margin="4,2" Background="#66FFFFFF" />
          <Button x:Name="BtnMosaic" Content="{DynamicResource Screenshot_Annotate_Mosaic}" Style="{StaticResource ToolbarButton}" Click="BtnMosaic_Click" ToolTip="{DynamicResource Screenshot_Tooltip_Annotate_Mosaic}" />
          <Button x:Name="BtnUndo" Content="{DynamicResource Screenshot_Annotate_Undo}" Style="{StaticResource ToolbarButton}" Click="BtnUndo_Click" ToolTip="{DynamicResource Screenshot_Tooltip_Annotate_Undo}" />
          <Button x:Name="BtnCancel" Content="{DynamicResource Screenshot_Cancel}" Style="{StaticResource ToolbarButton}" Click="BtnCancel_Click" ToolTip="{DynamicResource Screenshot_Tooltip_Cancel}" />
        </StackPanel>
      </Border>
    </Canvas>
  </Grid>
</Window>
