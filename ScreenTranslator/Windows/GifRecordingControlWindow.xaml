<Window x:Class="ScreenTranslator.Windows.GifRecordingControlWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="{DynamicResource GifRecording_Title}"
        Width="360"
        Height="76"
        WindowStyle="None"
        ResizeMode="NoResize"
        ShowInTaskbar="False"
        WindowStartupLocation="Manual"
        Topmost="True"
        ShowActivated="False"
        Background="Transparent"
        AllowsTransparency="True">
  <Border Background="#EE1F1F1F" BorderBrush="#55333333" BorderThickness="1" CornerRadius="8" Padding="10,8">
    <Grid>
      <Grid.ColumnDefinitions>
        <ColumnDefinition Width="*" />
        <ColumnDefinition Width="Auto" />
      </Grid.ColumnDefinitions>

      <TextBlock Grid.Column="0"
                 x:Name="HintText"
                 Foreground="#D8FFFFFF"
                 VerticalAlignment="Center"
                 TextTrimming="CharacterEllipsis"
                 Margin="0,0,12,0" />

      <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
        <Button x:Name="StopButton" MinWidth="72" Margin="0,0,6,0" />
        <Button x:Name="CancelButton" MinWidth="72" />
      </StackPanel>
    </Grid>
  </Border>
</Window>
